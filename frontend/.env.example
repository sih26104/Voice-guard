# Base URL of the Spring Boot backend (no trailing slash).
# Used by the frontend for the POST /api/analyze call.
# Default when unset: the Vite dev proxy forwards /api to http://127.0.0.1:8080
VITE_API_URL=http://127.0.0.1:8080
